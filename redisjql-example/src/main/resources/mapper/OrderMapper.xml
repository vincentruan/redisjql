<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sqlfans.redisjql.example.mapper.OrderMapper">
    <!-- 结果映射 -->
    <resultMap id="orderResultMap" type="Order">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="totalPrice" column="total_price"/>
        <result property="orderTime" column="order_time"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="version" column="version"/>
    </resultMap>
    
    <!-- 订单详情结果映射 -->
    <resultMap id="orderDetailResultMap" type="OrderDetail">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="totalPrice" column="total_price"/>
        <result property="orderTime" column="order_time"/>
        <result property="status" column="status"/>
        <result property="userName" column="user_name"/>
        <result property="productName" column="product_name"/>
        <result property="productCategory" column="product_category"/>
        <result property="productPrice" column="product_price"/>
    </resultMap>
    
    <!-- 公共字段 -->
    <sql id="Base_Column_List">
        id, user_id, product_id, quantity, total_price, order_time, status, create_time, update_time, version
    </sql>
    
    <!-- 查询操作 -->
    <select id="findById" resultMap="orderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE id = #{id}
    </select>
    
    <select id="findByUserId" resultMap="orderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE user_id = #{userId}
        ORDER BY order_time DESC
    </select>
    
    <select id="findByProductId" resultMap="orderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE product_id = #{productId}
        ORDER BY order_time DESC
    </select>
    
    <select id="findByStatus" resultMap="orderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE status = #{status}
        ORDER BY order_time DESC
    </select>
    
    <select id="findByOrderTimeRange" resultMap="orderResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_order
        WHERE order_time BETWEEN #{startTime} AND #{endTime}
        ORDER BY order_time DESC
    </select>
    
    <!-- 联表查询的订单详情 -->
    <select id="findOrderDetailsByUserId" resultMap="orderDetailResultMap">
        SELECT 
            o.id, o.user_id, o.product_id, o.quantity, o.total_price, o.order_time, o.status,
            u.name AS user_name,
            p.product_name, p.category AS product_category, p.price AS product_price
        FROM t_order o
        JOIN t_user u ON o.user_id = u.id
        JOIN t_product p ON o.product_id = p.id
        WHERE o.user_id = #{userId}
        ORDER BY o.order_time DESC
    </select>
    
    <select id="findOrderDetailsByProductId" resultMap="orderDetailResultMap">
        SELECT 
            o.id, o.user_id, o.product_id, o.quantity, o.total_price, o.order_time, o.status,
            u.name AS user_name,
            p.product_name, p.category AS product_category, p.price AS product_price
        FROM t_order o
        JOIN t_user u ON o.user_id = u.id
        JOIN t_product p ON o.product_id = p.id
        WHERE o.product_id = #{productId}
        ORDER BY o.order_time DESC
    </select>
    
    <select id="findOrderDetailsByUserIdAndProductId" resultMap="orderDetailResultMap">
        SELECT 
            o.id, o.user_id, o.product_id, o.quantity, o.total_price, o.order_time, o.status,
            u.name AS user_name,
            p.product_name, p.category AS product_category, p.price AS product_price
        FROM t_order o
        JOIN t_user u ON o.user_id = u.id
        JOIN t_product p ON o.product_id = p.id
        WHERE o.user_id = #{userId} AND o.product_id = #{productId}
        ORDER BY o.order_time DESC
    </select>
    
    <!-- 插入操作 -->
    <insert id="insert" parameterType="Order">
        INSERT INTO t_order (
            id, user_id, product_id, quantity, total_price, order_time, status, create_time, update_time, version
        ) VALUES (
            #{id}, #{userId}, #{productId}, #{quantity}, #{totalPrice}, #{orderTime}, #{status}, 
            #{createTime}, #{updateTime}, #{version}
        )
    </insert>
    
    <!-- 更新操作 -->
    <update id="update" parameterType="Order">
        UPDATE t_order 
        SET user_id = #{userId},
            product_id = #{productId},
            quantity = #{quantity},
            total_price = #{totalPrice},
            order_time = #{orderTime},
            status = #{status},
            update_time = #{updateTime},
            version = version + 1
        WHERE id = #{id} AND version = #{version}
    </update>
    
    <update id="updateStatus">
        UPDATE t_order 
        SET status = #{status},
            update_time = CURRENT_TIMESTAMP,
            version = version + 1
        WHERE id = #{id} AND version = #{version}
    </update>
    
    <!-- 删除操作 -->
    <delete id="deleteById">
        DELETE FROM t_order
        WHERE id = #{id}
    </delete>
</mapper> 