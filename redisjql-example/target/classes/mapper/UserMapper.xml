<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sqlfans.redisjql.example.mapper.UserMapper">
    <!-- 结果映射 -->
    <resultMap id="userResultMap" type="User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="gender" column="gender"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="address" column="address"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="version" column="version"/>
    </resultMap>
    
    <!-- 公共字段 -->
    <sql id="Base_Column_List">
        id, username, name, age, gender, email, phone, address, create_time, update_time, version
    </sql>
    
    <!-- 查询操作 -->
    <select id="findById" resultMap="userResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        WHERE id = #{id}
    </select>
    
    <select id="findByName" resultMap="userResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        WHERE name = #{name}
    </select>
    
    <select id="findByAgeRange" resultMap="userResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        WHERE age BETWEEN #{minAge} AND #{maxAge}
        ORDER BY age ASC
    </select>
    
    <select id="findByGender" resultMap="userResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM t_user
        WHERE gender = #{gender}
        ORDER BY name ASC
    </select>
    
    <!-- 插入操作 -->
    <insert id="insert" parameterType="User">
        INSERT INTO t_user (
            id, username, name, age, gender, email, phone, address, create_time, update_time, version
        ) VALUES (
            #{id}, #{username}, #{name}, #{age}, #{gender}, #{email}, #{phone}, #{address}, 
            #{createTime}, #{updateTime}, #{version}
        )
    </insert>
    
    <!-- 更新操作 -->
    <update id="update" parameterType="User">
        UPDATE t_user 
        SET username = #{username},
            name = #{name},
            age = #{age},
            gender = #{gender},
            email = #{email},
            phone = #{phone},
            address = #{address},
            update_time = #{updateTime},
            version = version + 1
        WHERE id = #{id} AND version = #{version}
    </update>
    
    <!-- 删除操作 -->
    <delete id="deleteById">
        DELETE FROM t_user
        WHERE id = #{id}
    </delete>
</mapper> 